# 2025-12-07 Edits

- Swapped client transport to `httpx.AsyncClient`, removing `aiohttp` loop dependency and aligning auth/token handling.
- Updated client API modules and tests to use `_request`/httpx responses; refreshed segment/object/tag/deletion tests.
- Added integration test guard with `TAMS_TEST_SERVER_BASE`/`TAMS_TEST_SERVER_URL` env vars (e.g., `docker1:8000`); skip when unreachable.
- Updated client README requirements/testing instructions (Python 3.12+, httpx) and noted server env for integration.

